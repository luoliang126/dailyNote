/**
 * Created by acer on 2017/9/16.
 */
app.controller("algorithmController",function(){
    //数组去重1
    //var arr = [2,56,4,89,4,343,465,67,2,3,5,7,565,123];
    //for(var i=0;i<arr.length;i++){
    //    for(var j=arr.length;j>i;j--){
    //        if(arr[i] == arr[j]){
    //            arr.splice(j,1);
    //        }
    //    }
    //}
    //console.log(arr);

    //数组去重2
    //var arr = [2,56,4,89,4,343,465,67,2,3,5,7,565,123];
    //var arrNew = [];
    //for(var i = 0;i<arr.length;i++) {
    //    for (var j = 0; j < arrNew.length; j++) {
    //        if (arr[i] == arrNew[j]) {
    //            break;
    //        }
    //    }
    //    if (j == arrNew.length) {
    //        arrNew.push(arr[i]);
    //    }
    //}
    //console.log(arrNew);

    //数组去重3 (先将数组排序，然后再相邻两个进行比较，该方法会有一个先排序的动作，使用时根据需要选用)
    //var arr = [2,56,4,89,4,343,465,67,2,3,5,7,565,123];
    //var arrNew = [];
    //arr = arr.sort(function (a,b) {
    //    return a - b;
    //});
    //for(var i=0;i<arr.length;i++){
    //    if(arr[i] != arr[i+1]){
    //        arrNew.push(arr[i])
    //    }else{
    //
    //    }
    //}
    //console.log(arrNew);

    //数组去重4（将数组转化为对象，并对该值赋值1。比如：arr=[1,2,3] 转为对象: temp={"1":1,"2":1,"3":1}）
    var arr = [2,56,4,89,4,343,465,67,2,3,5,7,565,123];
    var temp = {};
    var arrNew = [];
    for(var i = 0;i<arr.length;i++){
        if(!temp[arr[i]]){      //此处的temp对象不能用 temp.arr[i],当对象的键是一个number类型是应该为temp={"1":1,"2":1,"3":1}，访问时用temp[1];
            arrNew.push(arr[i])
            temp[arr[i]] = 1;
        }
    }
    console.log(arrNew);
})

